# MultiBrain Lab — PLAN

作成日：2026-09-16／版：0.1／状態：実装前の計画

## 1. 目的と範囲

**ハエの神経配線を使った複数の神経モデルを、一つの人型身体に接続する。床から起き上がって立てるまでの学習過程を、単脳・多脳の対照実験で並列に観察する。**

調べるのは、脳の数、身体の担当範囲、直接通信の有無によって、学習速度・安定性・協調がどう変わるか。美少女アバターは身体と学習過程を見せるために使用する。

「何日で立つか」は測定する結果であり、納期や性能目標にしない。以前の議論にあった「数時間〜数日で習得」「数百〜数千体を並列実行」は未検証のため採用しない。

### 前提

- 使用するのは、実在するハエから再構成された神経配線データ。個体の経験や記憶を復元した学習済みモデルではない。一方で、生物由来の構造まで白紙という意味でもない。[S1]
- 神経の計算方法、人体への接続、報酬による更新は、このプロジェクトが設計する。生きたハエの完全再現、意識の再現、実在する多脳動物の構造の証明は主張しない。
- 初版は起き上がりと立位に限定する。歩行、視覚学習、会話、LLM、世代進化、配信サービスは後続課題とする。

## 2. 初版で採用する構成

| 項目 | 決定 |
|---|---|
| 計算環境 | ユーザー指定のRTX 5090＋Ryzen 9 9950X3D。GPUメモリは32GB。[S6] |
| 基礎データ | MaleCNS v1.0。公式の神経細胞情報・接続表・神経伝達物質の予測を取得する。[S1][S2] |
| 神経モデル | 配線を固定した疎な再帰ネットワーク。初版は活動量を連続値で表す簡略モデルとする。 |
| 学習範囲 | 神経ごとの増幅率、小さな入力層・出力層、通信の入出力層。接続先の追加・削除は行わない。 |
| 強化学習 | PPOを基礎にした協調学習。学習時だけ全身の状態を評価するMAPPO方式を採用する。[S3][S4] |
| 物理計算 | MuJoCo Warp。表示を省いたGPU並列計算とPyTorchとの接続を使う。[S5] |
| 表示 | Three.js＋VRM。物理状態を読み取る表示専用の仕組みに分離する。 |

ここでいう「一脳」は、MaleCNS由来の配線を一組使用した計算単位を指す。MaleCNSは脳と腹側神経索を含むため、厳密には中枢神経系由来のモデルである。[S1]

発火の一回一回を扱うLIFモデルは初版では採用しない。まず配線構造と多脳化の比較を成立させ、その後、計算方法を変更した別実験として検証する。**「生物の発火を忠実に再現したモデル」と表示しない。**

## 3. 並列に比較する条件

同一の身体・観測・報酬・開始姿勢を使う。三脳はそれぞれ独立した学習パラメータと内部状態を持つ。

| 条件 | 脳と身体の接続 | 主に調べること |
|---|---|---|
| `mono` | 一脳が全身を操作 | 単脳の基準 |
| `tri_symmetric` | 三脳が全身への操作案を出し、等しい比率で平均 | 操作権限が重なる場合の協調・競合 |
| `tri_body` | 左側・中央・右側に操作関節を固定分割。直接通信なし | 身体を通じた協調 |
| `tri_linked` | `tri_body`と同じ分割に、遅延付きの通信を追加 | 直接通信の効果 |
| `mlp_control` | 通常の小さな人工ニューラルネットが全身を操作 | 身体・報酬・学習実装が成立するか |

左右担当は同側の腕・脚の関節、中央担当は可動する体幹・首の関節を操作する。股関節は左右担当に含める。骨盤の位置・向きを直接動かす操作は設けない。対応表は`joint_groups.yaml`に固定する。

初版では全条件・全脳に同じ全身の身体感覚を渡す。観測範囲まで同時に変えて原因を混ぜない。他脳の内部状態・操作案は渡さず、`tri_linked`だけが明示的な通信を受け取る。

**解釈上の制約：** `tri_body`の左右分担は設計者が与えたものなので、分業の自然発生とは呼ばない。自発的な役割分化は、主に`tri_symmetric`で調べる。`mono`との比較には計算規模の差も含まれる。

主比較が動作した後、三脳MLP、学習を停止した神経中核、接続を入れ替えた同規模ネットワークを追加する。ハエ特有の配線の有用性を主張するには、これらの対照と学習可能パラメータ数・計算予算の確認が必要となる。

## 4. 身体・課題・成功判定

### 身体

一つの共通物理モデルを全条件で使う。質量、関節範囲、摩擦、関節に与えられる力の上限を固定する。床以外の支持物は置かない。

入力は関節角度・速度、身体から見た重力方向、胴体の速度、足・手などの接触状態。画像入力は使用しない。出力は上限付きの関節トルクとし、起立動作、姿勢維持装置、歩行アニメーションを組み込まない。

初期設定は物理更新200Hz、操作更新50Hzとする。安定性試験で変更した場合は全条件へ同じ設定を適用し、本比較前に固定する。

### 課題を二つに分ける

| 課題 | 開始状態 | 扱い |
|---|---|---|
| `stand_balance` | 立位付近に小さな揺らぎを与える | 実装確認用。起き上がり成功には数えない。 |
| `rise_and_stand` | 仰向け・うつ伏せ・横向きで床上から開始 | 本課題。未学習の重みから開始する。 |

`stand_balance`の学習済み重みを本比較へ持ち込まない。段階的に易しい姿勢から学ばせる方式を後で試す場合は別条件とし、その事前学習も経験量に含める。

`rise_and_stand`は床への接地や転倒だけで試行を終了せず、再び起き上がる機会を残す。試行の終了条件は時間上限・場外・数値異常などとして別に定義する。

### 本課題の成功

60秒以内の試行中に、次の状態を**連続10秒**満たすことを一試行の成功とする。以下の値は初期案であり、物理モデルの検証後、本比較の前に固定する。

- 両足が床に接し、足以外は床に接していない。
- 胴体の傾きが鉛直から15度以内、骨盤高が基準立位の90%以上。
- 身体の水平移動速度が0.2m/s以下で、跳躍や転倒途中ではない。

起き上がる途中の手・膝の接地は許可する。学習中の「初めて1秒・3秒・10秒立った」は観察用の節目として別に保存する。

学習に使わない24個の開始条件で評価し、20試行以上成功した評価が2回続いた時点を「習得」とする。初成功と習得を区別する。評価間隔も全条件で同じ身体操作ステップ数に固定する。

### 報酬

高さ・胴体の向きの改善、立位維持時間に報酬を与え、過大なトルクと急激な操作変化を抑える。全脳に同じ身体全体の報酬を返す。共有報酬によって協調するかどうかも検証対象とする。

係数はMLPによる事前検証で決め、本比較中に個別調整しない。跳び続ける、膝立ちで止まる、床に引っ掛かるだけで高評価になる、といった抜け道を試験する。報酬の高さと成功判定は分離する。

## 5. 神経モデルと学習の境界

### データの固定

公式データを取得し、採用する神経細胞集合と、その間の接続を固定する。大型の電子顕微鏡画像や三次元形状データは学習に不要なので取得対象にしない。[S2]

`dataset_manifest.json`に、取得元・版・ファイルのハッシュ・抽出条件・ニューロン数・神経間の接続数・元のシナプス数・除外した要素を保存する。**シナプスの個数と、同じ神経間の接続をまとめた行列要素数を混同しない。** 過去の会話に出た接続数は定数として埋め込まず、取得したデータから算出する。

神経伝達物質の予測から信号の符号を決める方法、予測不明の場合の扱い、接続強度の正規化はモデル上の仮定として記録する。入力・出力に使う神経IDは`ports.json`に固定し、入力から出力へ信号が届く経路も検査する。

### 初版の神経計算

```text
身体感覚 → 小さな入力層 → 配線を固定した神経中核 → 小さな出力層 → 関節操作
                              ↑ 内部状態を保持
```

神経中核は、例えば次の形の連続値モデルとして実装する。

```text
x_next = (1 - alpha) * x + alpha * sigmoid(W0 @ (g * x) + U @ observation + b)
```

`W0`は元の配線から作る固定の疎行列、`x`は環境ごとに異なる神経活動、`g`は神経ごとの学習可能な増幅率。`alpha`と`b`は共通設定、`U`は入力ポートだけへ接続する小さな入力層とする。通信も指定した入力ポートへ入れる。

`g`は正の有限範囲に制約し、初期案は0.5〜2.0とする。配線と符号を維持しながら、神経中核の振る舞いも学習で変化させる。活動量の更新は操作ごとに一回から検証し、生物学的な時間尺度とは同一視しない。

身体感覚から関節操作へ直接抜ける経路や、大きな別ネットワークは設けない。入力・読出しのポート数と学習可能パラメータ数を記録する。神経中核を学習停止にした対照との差を測り、「中核が学習したこと」と「周囲の層だけで適応したこと」を分ける。

### 学習処理

PyTorch／TorchRLを使い、各脳の操作を選ぶモデルは独立させる。身体全体を評価するモデルは学習時だけ使用し、実行時の動作には介入しない。[S3][S4]

時間順の短い区間で学習し、初期案は16操作ステップとする。区間開始時の内部状態、必要な直前履歴、過去の操作確率を保存・再計算し、時系列を一枚ずつ無作為に混ぜない。全神経・全時刻の計算履歴を無制限にGPUへ保持しない。

`tri_symmetric`では、各脳が実際に出した操作案とその確率を記録して学習する。平均後の身体操作を、各脳が直接選んだ操作として扱わない。平均で初期の探索幅が変わるため、実際の関節トルクの分布を事前測定して比較条件に記録する。

### 脳間通信

`tri_linked`は各脳から他の二脳へ8次元の連続値を送り、次の操作ステップで受信する。値の範囲、送信頻度、送信元の識別を固定する。これは人工的な通信経路であり、「8ニューロン」や情報量8bitとは呼ばない。

通信値も学習する操作の一部として出力し、PPOの操作確率に含める。受信値は神経中核への入力に限定する。リセット時には通信待ち行列も消去する。通信をゼロにする・時系列を入れ替える評価を行い、通信の追加による容量増加と、通信内容の有用性を区別する。

## 6. 5090上の並列実行と計測

### 三つの単位を混同しない

| 単位 | 共有するもの | 独立させるもの |
|---|---|---|
| 同一条件・同一試行の並列環境 | 学習中のモデル | 身体、神経活動、乱数、通信状態 |
| 一つの身体に載る三脳 | 固定の元配線だけ | 学習パラメータ、神経活動、操作出力 |
| 条件違い・独立した再試行 | 元データと実験仕様 | 重み、経験、最適化状態、正規化統計 |

多数の環境から一つのモデルへ経験を集めても、独立した再試行の数が増えたことにはならない。本比較は各条件につき3種類の学習用乱数から始め、個々の結果をすべて表示する。

### 実行方式

一つの管理プロセスで5条件を起動し、各条件の収集・更新を小さな単位で交互に進める。読み取り専用の元配線を共有し、条件間の学習データは分離する。各条件を最後まで順番に走らせる方式にはしない。

単一GPU上で全条件の計算が常に物理的に同時実行されることは要求しない。まず全条件を継続して進める並行スケジュールを実現し、余裕がある部分をまとめてGPU処理する。一度に全乱数試行が載らない場合も、各試行の組の中では5条件を並行して進める。

### 最初に測ること

各条件1環境から始め、全条件を載せた状態で2・4・8…と増やす。**推論だけでなく、物理計算、経験収集、神経中核の勾配計算、更新、評価まで含めて測定する。**

記録する値は、最大GPUメモリ、ホストメモリ、条件別の身体操作ステップ/秒、更新時間、保存時間、表示の有無。GPUメモリに15〜20%の余裕を残す初期方針とし、最大個体数だけを目的にしない。

メモリには元配線だけでなく、全環境の神経状態、時間方向の計算履歴、学習パラメータ・勾配・最適化状態、物理状態が含まれる。元配線を共有しても、独立した三脳の計算量が一脳分になるわけではない。

`benchmark.json`から並列数を決め、長時間実行前に`max_body_steps`と`max_compute_hours`を必須設定する。未設定なら長時間実行を開始しない。数値異常・メモリ不足ではチェックポイントを保存して停止する。

### 「何日」と公平性

経過した実時間、条件に割り当てた実計算時間、累積の身体操作ステップ、合計の仮想経験時間を分けて記録する。三脳が同じ身体を一回操作した経験は一ステップとして数え、三ステップにはしない。

経験量をそろえた曲線と、計算時間をそろえた曲線を両方作る。計算時間は他条件と重ならない計測区間で集計し、GPUを使う評価・表示の負荷も別記する。比較群間で調整に使う試行数もそろえる。

## 7. 記録と可視化

最低限、成功率、連続立位時間、起き上がり時間、姿勢・高さ、接触、トルク、各脳の出力と活動の集計、通信値、三種類の時間指標を保存する。全時刻の全ニューロン活動は保存せず、集計値・抽出した神経・節目の短い記録に絞る。

各条件を一体ずつ横並びで表示し、同じ学習経験量のチェックポイントを再生できるようにする。実時間表示と学習量をそろえた再生を区別する。選択個体の表示が停止しても学習は続けられる構成とする。

VRMは保存された物理姿勢を表示する。演出で立位を補正せず、実測値に基づいて節目を表示する。VRMモデルの利用・配信・再配布条件とクレジットを確認し、許可のないモデルは同梱しない。[S7]

協調・役割分化は活動の相関だけで判断しない。評価用コピーで一脳の出力を一時的にゼロにする、通信を遮断する、小さな外力を左右から与える、といった介入前後の性能変化を測る。介入の際に残りの出力を自動増幅しない。

設定、コードの版、データのハッシュ、乱数、モデル、最適化状態、正規化統計をチェックポイントへ保存する。継続用には身体・神経・通信の状態も含める。GPU計算の完全な一致は保証せず、同条件の再評価による再現性を確認する。[S5]

## 8. 実装の分割

以下は予定する構成。現在の納品物はPLAN.mdとREADME.mdのみ。

```text
configs/                 # 条件、課題、報酬、予算
src/multibrain/
  data/                  # データ取得・検証・疎行列化
  brain/                 # 神経計算、入力・出力、通信
  body/                  # 共通身体、観測、操作、成功判定
  learning/              # PPO/MAPPO、時系列の学習処理
  runner/                # 並行実行、計測、保存・再開
  evaluation/            # 共通評価、介入、比較集計
viewer/                  # VRM表示・保存記録の再生
assets/                  # 身体定義、利用条件を確認した表示資産
runs/                    # 設定・結果・記録。Git管理対象外
tests/                  # 単体試験、隔離試験、動作確認
```

Python、PyTorch、TorchRL、MuJoCo Warpの版は導入時の互換性試験で固定する。最初はLinux上のローカル実行を対象とし、他OS対応や独自CUDA実装は後回しにする。CPUは取得・前処理・管理・保存、GPUは神経計算・学習・物理計算を主に担当する。

## 9. 実装順序と完了条件

| 段階 | 作業 | 次へ進む条件 |
|---|---|---|
| M0：取得・実測 | データ固定、対応GPUでの疎行列の順伝播・逆伝播、メモリ測定 | `dataset_manifest.json`と`benchmark.json`ができ、全規模で一回の学習更新が通る |
| M1：身体の検証 | 共通身体、成功判定、MLPで二課題を試す | 保存した既知の姿勢への判定が正しく、MLPの改善を確認できる |
| M2：一脳 | 神経中核・入出力・時系列学習・保存再開 | 小規模試験で勾配を検証し、全規模でも中核の増幅率へ更新が届く |
| M3：三脳と並行実行 | 4神経条件＋MLP、操作分配、通信、独立状態 | 全条件が継続して進み、学習状態・経験・通信の混線がない |
| M4：比較と表示 | 共通評価、独立再試行、VRM再生、介入試験 | 同じ経験量で比較でき、初成功と未達成を含む結果を再確認できる |

各段階は小規模の試験を先に通す。ただし、縮小した神経回路や学習停止モデルの結果を、全規模・中核学習ありの結果として扱わない。

必須試験は、関節担当の重複・漏れ、成功判定の抜け道、三脳の独立性、環境リセット、通信遅延、時系列の操作確率、モデルの保存再開、表示を停止した場合の学習継続とする。

MLPが改善しなければ身体・報酬・学習処理を先に修正する。神経モデルだけが改善しなければ入力の到達性、活動の飽和、勾配、学習範囲を診断する。予算内で立てなかった場合は「未達成」と記録し、学習不能や特定構造の劣等性まで断定しない。

**初版の完了は、全条件を並行学習でき、成功・失敗と協調の変化を保存・比較・再生できること。全条件が立てること自体は完了条件にしない。**

後続では、通信の次元数、身体感覚の局所化、配線を入れ替えた対照、LIFモデル、歩行へ順に広げる。一回の比較で変更する要因は限定する。

## 参考資料

確認日：2026-09-16。公開資料に基づく事実と、この計画で提案した設計を区別する。上記の学習方式・身体制御の組合せは、本プロジェクトで検証するものであり、既存研究による動作保証はない。

- **[S1] HHMI Janelia — Male CNS Connectome**：対象範囲、公開版、利用条件。公式記録ではv1.0公開は2026-06-08、正式論文公開は2026-09-03。
  `https://www.janelia.org/project-team/flyem/male-cns-connectome`
- **[S2] MaleCNS公式ダウンロード**：神経情報、伝達物質予測、集約済み接続表。
  `https://male-cns.janelia.org/download/`
- **[S3] Yu et al. — The Surprising Effectiveness of PPO in Cooperative, Multi-Agent Games**：協調型PPOの基礎。
  `https://arxiv.org/abs/2103.01955`
- **[S4] TorchRL公式 — Multi-Agent Reinforcement Learning (PPO)**：中央評価と分散した行動選択、パラメータ共有の設定。
  `https://docs.pytorch.org/rl/stable/tutorials/multiagent_ppo.html`
- **[S5] MuJoCo公式 — MuJoCo Warp**：GPU並列物理計算、PyTorchとの接続、再現性の制約。物理計算の微分は前提にしない。
  `https://mujoco.readthedocs.io/en/stable/mjwarp/index.html`
- **[S6] NVIDIA — GeForce RTX 5090**：32GB GPUメモリ等の公式仕様。
  `https://www.nvidia.com/en-us/geforce/graphics-cards/50-series/rtx-5090/`
- **[S7] VRM Public License 1.0**：モデルごとの利用条件と表示条件。
  `https://vrm.dev/en/licenses/1.0/`
