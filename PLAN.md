# MultiBrain Lab 実装計画

作成日：2026-09-16／版：0.3／状態：M0 進行中

版 0.3 では、M0 で MaleCNS v1.0 の実データと計算環境を確認し、次の点を改めた。

- ニューロン集合の規則を `status == Traced` に基づくものに直し、細胞体位置による除外をやめた（§4.1）。
- 腕の対応先を前脚（T1）から中脚（T2）に変え、運動ニューロン群が小さい関節の集約規則を追加した（§4.3、§4.4）。
- 規模を実測値（N = 71,618、nnz = 8,053,558）に置き換え、GPU を RTX 5080（16 GB）として並列数の基準を条件あたり 64 環境にした（§4.5、§10）。

版 0.2 では、版 0.1 の骨格を保ったまま次の点を改めた。

- 神経モデルを、膜電位と入力電流の二変数で記述する更新式に置き換え、連続値版と LIF 版が同じ式を共有するようにした（§4）。
- 入出力を「小さな層」と呼ぶのをやめ、MaleCNS の注釈から入力ポートと出力ポートを機械的に選ぶ規則を書いた（§4.3、§4.4）。
- 人型身体を MuJoCo XML に落とせる解像度で定義した（§3）。
- 脳間通信を、行動として出力する連続値ベクトルから、脳をまたぐ合成シナプス結合に変えた（§6）。
- 三脳の競合と協調を文章として監視する仕組みと、視覚入力の経路を設計に含めた（§8、§9）。
- 到達目標を経験量の予算つきで書いた（§1）。
- MaleCNS の注釈クラスと役割の対応表（§4.3）と、身体定義の骨格 XML（§3.7）を置いた。

## 1. 目的と到達目標

**MaleCNS 由来の配線を持つ神経モデルを一つの人型身体に接続し、床から起き上がって立つ動作を強化学習で獲得させる。**
比較の軸は、脳の数（一つか三つか）、三脳の身体分担の有無、脳間の合成シナプス結合の有無の三つである。
美少女アバターは、身体と学習過程を見せるための表示に使う。

初版の到達目標は次の四つとする。
予算の単位は、身体を一回操作する **身体ステップ**（20 ms）である。

- **G1 身体と学習系の成立**：通常の MLP 方策（`mlp_control`）が、`rise_and_stand` 課題を 2×10^8 身体ステップ以内に習得する。
- **G2 一脳の起立**：一脳条件（`mono`）が、同じ課題を 5×10^8 身体ステップ以内に習得する。
- **G3 三脳の比較**：三脳条件三種を G2 と同じ予算で走らせ、習得の有無にかかわらず、学習曲線、協調指標、結合指標を含む比較結果を出す。
- **G4 LIF 版への移行**：出力関数を発火に切り替えた LIF 版が、`stand_balance` 課題を 1×10^8 身体ステップ以内に習得する。

予算内に達しなかった条件は「未達成」として同じ形式で記録し、§12 の診断手順で原因を切り分ける。
「習得」の定義は §5 に置く。

予算の目安は公開実装から取った。
MLP 方策による人型の立位維持は 6×10^7 身体ステップ、2,048 環境の設定で公開されており [S18]、床からの起き上がりを実機向けに学習した研究は 4,096 環境で第一段階に 5×10^9 シミュレーションステップ（物理更新と制御更新のどちらの単位かは原典で要確認）を使っている [S17]。
G1 の 2×10^8 はこの間に置いた値で、単純化した身体と乱択化なしの設定を前提にしている。
MLP がこの予算で立てなければ、G1 の予算を一度だけ引き上げ、変更を版として記録する。

初版の範囲は起き上がりと立位に限る。
歩行、視覚学習、世代進化、配信は後続課題とするが、視覚入力については初版の設計に接続点を用意する（§9）。

## 2. 比較条件

全条件で同じ身体、観測、報酬、開始姿勢を使う。
ここでいう **脳** は、MaleCNS 由来の配線一組（脳と腹側神経索を含む）を使った計算単位である。
三脳の各脳は、固定配線だけを共有し、学習パラメータと内部状態を独立に持つ。

| 条件 | 脳と身体の接続 | 主に調べること |
|---|---|---|
| `mono` | 一脳が 27 自由度すべてを操作 | 単脳の基準 |
| `tri_symmetric` | 三脳がそれぞれ 27 自由度への操作案を出し、等しい比率で平均 | 操作権限が重なる場合の協調と競合 |
| `tri_body` | 左群 11、中央群 5、右群 11 の自由度を三脳に固定分割。合成シナプスなし | 身体を介した協調 |
| `tri_linked` | `tri_body` と同じ分割に、脳間の合成シナプス結合（§6）を追加 | 直接結合の効果と通信内容の創発 |
| `mlp_control` | 2 層 256 ユニットの MLP 方策が 27 自由度を操作 | 身体、報酬、学習実装の成立確認 |

自由度の群分けは §3.2 の表の「群」列で定める。
左群と右群は同側の肩、肘、手首、股、膝、足首、中央群は腰と首である。
骨盤の位置と向きを直接動かす操作は設けない。
対応表は `configs/joint_groups.yaml` に固定し、重複と漏れを試験で検査する。

全脳に同じ全身の観測（§3.5）を渡す。
他脳の内部状態や操作案は渡さず、`tri_linked` だけが合成シナプスを通じて他脳の活動を受け取る。

`tri_body` の左右分担は設計者が与えるので、自発的な役割分化は `tri_symmetric` で調べる。
`mono` と三脳条件の比較には計算規模の差が含まれるため、パラメータ数を揃える対照（`mono_typed`）も置く。

主比較が動作した後、次の対照条件を追加する。

- **`tri_mlp`**：三つの MLP 方策で `tri_body` と同じ分割を行う。三脳化の効果を配線から切り離す。
- **`mono_frozen`**：神経中核の学習を止め、ポートと読出しだけを学習する。中核が学習したことと、周辺だけで適応したことを分ける。
- **`mono_shuffled`**：入次数と出次数を保って接続先を入れ替えた同規模ネットワーク。初期化の乱数は `mono` と共有する。配線の優位が初期化の差や次数分布で説明できてしまう例が報告されているため [S22]、ハエ固有の配線の有用性を主張するために必要な対照である。
- **`mono_typed`**：学習パラメータを細胞型ごとに共有し、パラメータ数を減らした変種。配線の寄与とパラメータ数の寄与を分ける。

## 3. 身体の物理仕様

### 3.1 寸法と質量

身長 1.58 m、総質量 50 kg の人型を一体定義し、全条件で共用する。
体節の質量配分は de Leva の女性向け体節慣性パラメータ [S8] に従い、次の比率を初期値とする。
慣性テンソルは各体節の形状（カプセルまたは箱）から MuJoCo に自動計算させ、質量だけを指定する。

| 体節 | 質量比 | 質量（kg） | 形状 |
|---|---|---|---|
| 頭（首を含む） | 6.7% | 3.3 | 球、半径 0.09 |
| 上部体幹（胸） | 15.5% | 7.7 | カプセル |
| 中部体幹（腹） | 14.7% | 7.3 | カプセル |
| 下部体幹（骨盤） | 12.5% | 6.2 | 箱 |
| 上腕（片側） | 2.6% | 1.3 | カプセル、長さ 0.26 |
| 前腕（片側） | 1.4% | 0.7 | カプセル、長さ 0.24 |
| 手（片側） | 0.6% | 0.3 | 箱 |
| 大腿（片側） | 14.8% | 7.4 | カプセル、長さ 0.38 |
| 下腿（片側） | 4.8% | 2.4 | カプセル、長さ 0.37 |
| 足（片側） | 1.3% | 0.65 | 箱、0.22×0.09×0.04 |

質量比の合計は 100% になる。
上部体幹と中部体幹は剛体木（§3.2）では一つの剛体 `torso` なので、XML では合算した 15.0 kg のカプセル一本にする。
体節長は身長 1.58 m に対する標準的な比率から取り、VRM モデルの骨格に合わせて ±10% の範囲で調整してよい。
表にない長さは M0 で次のように決めた：骨盤の箱 0.28×0.18×0.18、胴のカプセル 0.42、腰関節は骨盤中心の 0.12 上、首関節は胴の原点の 0.48 上、肩は胴の中心線から左右に 0.20。立位の骨盤高は 0.84 m、頭頂は 1.59 m になる。
これらは `src/multibrain/body/build_xml.py` の定数として置き、以後の比較中は変えない。

### 3.2 剛体木と関節

骨盤を根とする剛体木を次の順で組む。

```text
pelvis (free joint, 6 DoF)
├─ torso   : waist_z, waist_y, waist_x
│  ├─ head : neck_y, neck_z
│  ├─ upper_arm_L : shoulder_L_x, shoulder_L_y, shoulder_L_z
│  │  └─ forearm_L : elbow_L
│  │     └─ hand_L : wrist_L
│  └─ upper_arm_R, forearm_R, hand_R（同様）
├─ thigh_L : hip_L_x, hip_L_y, hip_L_z
│  └─ shin_L : knee_L
│     └─ foot_L : ankle_L_x, ankle_L_y
└─ thigh_R, shin_R, foot_R（同様）
```

操作する関節は 27 自由度で、すべて hinge とする。
身体座標系は x が前、y が左、z が上である。
関節名の末尾の x、y、z は回転の種類を表し、x が屈伸、y が外転と内転、z が内旋と外旋である。
したがって屈伸の回転軸は身体の y 軸で、実際の軸ベクトルは `configs/joint_groups.yaml` に 3 成分で書く。
軸の符号は、可動域の正の側がその関節の解剖学的な正方向（屈曲、外転、背屈）になるように選び、右側の外転と内外旋は左側の鏡像にする。
可動域、上限トルク、関節属性は次を初期値とする。
片側だけ示した関節は左右対称に定義する。

| 関節 | 軸 | 可動域（度） | 上限トルク（N·m） | 群 |
|---|---|---|---|---|
| `waist_z` | z | −40〜40 | 60 | 中央 |
| `waist_y` | y | −30〜30 | 80 | 中央 |
| `waist_x` | x | −60〜30 | 100 | 中央 |
| `neck_y` | y | −45〜45 | 15 | 中央 |
| `neck_z` | z | −60〜60 | 15 | 中央 |
| `shoulder_*_x` | x | −60〜170 | 40 | 左右 |
| `shoulder_*_y` | y | −30〜150 | 40 | 左右 |
| `shoulder_*_z` | z | −80〜80 | 25 | 左右 |
| `elbow_*` | x | 0〜150 | 30 | 左右 |
| `wrist_*` | x | −70〜70 | 8 | 左右 |
| `hip_*_x` | x | −30〜120 | 120 | 左右 |
| `hip_*_y` | y | −40〜40 | 80 | 左右 |
| `hip_*_z` | z | −40〜40 | 50 | 左右 |
| `knee_*` | x | 0〜150 | 120 | 左右 |
| `ankle_*_x` | x | −45〜30 | 80 | 左右 |
| `ankle_*_y` | y | −25〜25 | 30 | 左右 |

関節の共通属性は `armature="0.02"`、`damping="2"`、`frictionloss="0.2"` を初期値とし、`<default>` で一括指定する。
可動域の拘束は `solreflimit="0.01 1"`、`solimplimit="0.99 0.999 0.001"` で硬くする。既定値では ctrl = ±1 で可動域を 5 度ほど越えたためで、この設定で越え幅は 0.5 度未満になる。
上限トルクは MuJoCo 付属の humanoid モデル [S15]（質量約 41 kg、`gear` は腹 40、股の屈伸 120、膝 80、足首 20、肩 20、肘 40）を基準に、質量 50 kg への換算と、起き上がりで膝と足首と腰に大きな力が要ることを見込んで増やした値である。
M1 の身体検証で、上限トルクだけで起き上がれない、または簡単すぎる場合は表を改訂し、本比較の前に固定する。

### 3.3 アクチュエータ

全関節に、`gear` を上限トルク、`ctrlrange` を [−1, 1] としたトルク型アクチュエータを付け、正規化されたトルク指令を受ける。
方策の出力は、この正規化トルク 27 次元である。
関節位置サーボや歩行アニメーションは組み込まない。

トルク指令は時定数 40 ms の一次遅れで平滑化する。
MuJoCo Warp は一次遅れ（`dyntype="filter"`）を持つ `<general>` アクチュエータに対応している [S5] ので、`<general dyntype="filter" dynprm="0.04" gear="<上限トルク>" ctrlrange="-1 1">` として XML 側で実装する。
M0 で `put_model` がこの身体定義を受理することを確かめる。

### 3.4 接触形状と摩擦

床は `<geom type="plane">` 一枚とし、床以外の支持物は置かない。
体節の接触形状は §3.1 の形状列に従う。
足は箱一つで表し、足底全体を覆う箱型の `<site>` を接触検出用に置く。

摩擦係数は床と全体節で 1.0、`condim="3"` とする。
接触の柔らかさは `solref="0.01 1"`、`solimp="0.9 0.99 0.001"` とする。既定値では立位から倒れたときに頭部が床を 8 cm ほど一時的に貫通したためである。
床の `margin` は 0.01 とし、貫通対策には使わない。margin を大きくすると身体がその分だけ浮いて静止し、接触センサも床に触れる前に反応するので、成功判定の意味が変わってしまう。
MuJoCo Warp は箱同士の接触対に非零の margin を許さない（`io.py` で拒否される）ので、体節側の margin は 0 のままにし、`<flag multiccd="disable"/>` を置く。
隣接する親子体節の対は `<contact><exclude>` で接触計算から外し、それ以外の自己接触は有効にする。
接触の分類（足だけが床に触れているか）は `data.contact` を足の形状群とそれ以外に分けて数える。
M0 の実測では、乱数トルクで動かした 1 環境あたりの最大接触数は立位開始で 16、仰向け 24、うつ伏せ 24、横向き 20 で、`naconmax` の既定（1 環境あたり 48）で溢れなかった。

### 3.5 センサと観測ベクトル

観測は次の 76 次元とし、全条件、全脳に同じものを渡す。
角度は可動域で [−1, 1] に正規化し、速度は 10 rad/s で割って ±3 に切り詰める。

| 信号 | 次元 | MuJoCo センサ |
|---|---|---|
| 関節角度 | 27 | `jointpos` |
| 関節角速度 | 27 | `jointvel` |
| 骨盤座標系での重力方向 | 3 | `framequat`（骨盤）から計算 |
| 骨盤の角速度 | 3 | `gyro` |
| 骨盤の線速度 | 3 | `velocimeter` |
| 骨盤高さ（基準立位比） | 1 | `framepos` |
| 接触 | 12 | `touch`（両足、両手、両膝、両肘、骨盤、胸、背、頭） |

接触は接触力を 50 N で正規化して 1 に切り詰めた値とする。
直前の操作は観測に入れない。
画像は観測に入れない（視覚の拡張は §9）。

### 3.6 時間刻みとソルバ

物理更新は 5 ms（200 Hz）、身体ステップは 20 ms（50 Hz）で、身体ステップごとに物理更新を 4 回進める。
ソルバは MuJoCo Warp の既定（Newton）を使い、反復回数は M0 の安定性試験で決める。
安定性試験で刻みを変えた場合は全条件へ同じ設定を適用し、本比較の前に固定する。

M0 で 4 物理ステップを一つの CUDA グラフに取り込んで測った物理計算だけの速さは、64 環境で 4.0 ms/身体ステップ（1.6×10^4 身体ステップ/s）、1,024 環境で 6.6 ms（1.6×10^5 身体ステップ/s）だった（`data/body_check.json`）。
環境数を 16 倍にしても時間は 1.6 倍しか増えないので、物理計算は起動回数で決まり、環境数の上限は GPU メモリと神経中核側で決まる。

### 3.7 骨格 XML

§3.1 から §3.6 を MuJoCo XML に落とすと次の形になる。
左脚の連鎖だけを示し、右脚と体幹以下は同じ形式で書く。
実物は `src/multibrain/body/build_xml.py` が `configs/joint_groups.yaml` から生成する `assets/humanoid.xml`（214 行）で、M0 で MuJoCo と MuJoCo Warp の両方に受理された。
数値はすべて表の初期値で、M1 の検証で改訂したら XML と表を同時に直す。

```xml
<mujoco model="multibrain_humanoid">
  <option timestep="0.005"><flag multiccd="disable"/></option>
  <default>
    <joint type="hinge" armature="0.02" damping="2" frictionloss="0.2" limited="true"
           solreflimit="0.01 1" solimplimit="0.99 0.999 0.001"/>
    <geom condim="3" friction="1.0 0.005 0.0001" solimp="0.9 0.99 0.001" solref="0.01 1"/>
    <general dyntype="filter" dynprm="0.04" ctrlrange="-1 1" ctrllimited="true"/>
  </default>
  <worldbody>
    <geom name="floor" type="plane" size="0 0 1" margin="0.01"/>
    <body name="pelvis" pos="0 0 0.84">
      <freejoint/>
      <geom type="box" size="0.14 0.09 0.09" mass="6.2"/>
      <site name="imu"/>
      <body name="thigh_L" pos="0 0.09 -0.05">
        <joint name="hip_L_x" axis="0 -1 0" range="-30 120"/>
        <joint name="hip_L_y" axis="1 0 0" range="-40 40"/>
        <joint name="hip_L_z" axis="0 0 1" range="-40 40"/>
        <geom type="capsule" fromto="0 0 0 0 0 -0.38" size="0.06" mass="7.4"/>
        <body name="shin_L" pos="0 0 -0.38">
          <joint name="knee_L" axis="0 1 0" range="0 150"/>
          <geom type="capsule" fromto="0 0 0 0 0 -0.37" size="0.045" mass="2.4"/>
          <body name="foot_L" pos="0 0 -0.37">
            <joint name="ankle_L_x" axis="0 -1 0" range="-45 30"/>
            <joint name="ankle_L_y" axis="1 0 0" range="-25 25"/>
            <geom type="box" pos="0.04 0 -0.02" size="0.11 0.045 0.02" mass="0.65"/>
            <site name="foot_L_contact" type="box" pos="0.04 0 -0.02" size="0.11 0.045 0.02"/>
          </body>
        </body>
      </body>
      <!-- thigh_R、torso（waist_*、head、upper_arm_*）は同じ形式 -->
    </body>
  </worldbody>
  <contact>
    <exclude body1="pelvis" body2="thigh_L"/>
    <!-- 隣接する親子体節の対をすべて列挙 -->
  </contact>
  <actuator>
    <general name="hip_L_x" joint="hip_L_x" gear="120"/>
    <!-- 27 関節すべてに gear を §3.2 の上限トルクとして付ける -->
  </actuator>
  <sensor>
    <jointpos joint="hip_L_x"/>
    <jointvel joint="hip_L_x"/>
    <framequat objtype="site" objname="imu"/>
    <gyro site="imu"/>
    <velocimeter site="imu"/>
    <framepos objtype="site" objname="imu"/>
    <touch site="foot_L_contact"/>
    <!-- 27 関節と 12 接触部位に同様に付ける -->
  </sensor>
</mujoco>
```

関節名、群、上限トルクは `configs/joint_groups.yaml` からこの XML を生成するスクリプトで揃え、表と XML の食い違いを試験で検出する。

### 3.8 身体の検証項目

M1 では次を通す。

- 立位から静かに置いた身体が、操作なしで 2 秒以内に倒れ（骨盤高が初期の 70% 未満）、数値異常を出さず、倒れて静止した状態で床への沈み込みが 2 cm 以内である。倒れる途中の一時的な貫通は記録するだけで合否には使わない。
- 各関節を可動域の端まで動かしても、数値発散や接触の貫通が起きない。
- 保存した既知の姿勢（立位、膝立ち、仰向け、うつ伏せ、横向き）に対して成功判定と接触分類が正しい。
- `mlp_control` が G1 の予算内で習得する。

## 4. 神経モデルの数理設計

### 4.1 配線から結合行列を作る

MaleCNS v1.0 [S1][S2] の公式接続表から、次の規則で神経集合と結合行列を作る。
規則は `dataset_manifest.json` に記録し、抽出したニューロン数、接続数、元のシナプス数、除外した要素の数を残す。

- **ニューロン集合**：注釈の `status` が `Traced` のニューロン（165,122）から、`superclass` が `ol_intrinsic`（視覚葉内在ニューロン）と `ol_sensory`（光受容器）のものを除く。視覚葉から中央脳へ投射する視覚投射ニューロンは残す。M0 の実測でこの集合は 71,618 ニューロンになった。細胞体位置の有無は基準にしない。感覚ニューロンは細胞体が体の末梢にあって位置が記録されておらず、それを除外条件にすると入力ポートが消えるためである。集合内で接続を一本も持たないニューロン（713）は除かず、数だけ記録する。
- **接続の閾値**：ニューロン j から i へのシナプス数 n_ij が 2 未満の接続は捨てる。
- **符号**：前シナプス側ニューロン j の神経伝達物質予測から s_j を決める。ACh は +1、GABA、Glu、ヒスタミンは −1 とする[^glu]。ドーパミン、セロトニン、オクトパミンは初版では 0（速い伝達なし）とし、該当ニューロン数を記録する。予測の確信度が 0.5 未満のニューロンは、同じ細胞型の多数派の符号を使い、それもなければ +1 とする。
- **基準重み**：w0_ij = c · s_j · n_ij / sqrt(D_i)。D_i はニューロン i の入力シナプス総数である。c は M0 で、連続値版を動作点で線形化したときのスペクトル半径が 0.9 になるように決め、記録する。M0 では c = 1 の行列のスペクトル半径がべき乗法で 43.5 と出たので、出力関数の最大傾き 1/(4β) = 1 で線形化して c = 0.0207 とした（`data/benchmark_core.json`）。ただしこの c では静止点（v = 0、σ' ≈ 0.018）の実効利得が 0.016 しかなく、乱数観測を 50 ステップ与えても活動の平均は 0.028 で勾配は 10^−6 台にとどまった。動作点をどこに置くか（バイアス b の初期値、または平均活動を 0.1〜0.2 に合わせる c の再調整）は M2 の最初の作業とし、§12.3 の手順 2 で扱う。

[^glu]: ハエではグルタミン酸がグルタミン酸作動性塩化物チャネルを介して主に抑制性に働くため、符号を負にする。

符号は前シナプス側で決まるため、Dale の法則が構造として保たれる。
学習で符号は変わらない。
接続先の追加と削除も行わない。

正規化を入次数の平方根で行うのは、入力の多いニューロンが入力を積算する性質を保ちつつ、入力規模の差による活動の飽和を抑えるためである。
入次数で割る正規化と正規化なしは、M2 の小規模試験で対照として試し、選んだ方式を記録する。

### 4.2 状態更新式と LIF 版への移行

各ニューロン i は膜電位 v_i と入力電流 I_i の二つの状態を持つ。
膜電位は発火閾値を 1、静止電位を 0 とする無次元単位で扱う。
時間刻み Δt の離散更新は次のとおりである。

```text
I_i[t+1] = γ_i · I_i[t] + (1 − γ_i) · ( Σ_j W_ij · s_j[t] + u_i[t] + m_i[t] )
v_i[t+1] = α_i · v_i[t] + (1 − α_i) · ( I_i[t+1] + b_i )
α_i = exp(−Δt / τm_i),  γ_i = exp(−Δt / τs_i)
W_ij = g_i · w0_ij
```

u_i は入力ポートへの感覚電流（§4.3）、m_i は合成シナプスからの電流（§6）で、ポート以外のニューロンでは 0 である。
出力 s_i の定め方だけが、連続値版と LIF 版で異なる。

- **連続値版（初版）**：s_i[t+1] = σ((v_i[t+1] − 1) / β)。σ はロジスティック関数、β は 0.25 で固定する。Δt は身体ステップと同じ 20 ms とし、神経更新は身体ステップごとに一回である。
- **LIF 版（G4）**：s_i[t+1] = H(v_i[t+1] − 1)。H は階段関数で、発火したニューロンは v_i から 1 を引き（減算リセット）、2 ms の不応期に入る。Δt は 1 ms とし、身体ステップごとに神経更新を 20 回進める。逆伝播では H の微分を高速シグモイド関数の微分で置き換える代理勾配法 [S9] を使い、傾きは snnTorch の既定値 25 [S21] を出発点に M5 で調整する。読出しには発火列を時定数 20 ms で平滑化した痕跡を使う。

二つの版は、出力関数と時間刻みを差し替えるだけで同じ実装を共有する。
LIF 版の出力関数を σ に差し替え、リセットと不応期を外すと連続値版と同じ軌道になることを単体試験で確かめ、移行の整合性を保証する。

連続値版を先に置く理由は、全脳配線を非発火のグラフ方策として身体制御に使った先行研究 [S16] があり、同じ研究の付録で LIF 版の基準実装が学習に失敗した報告があるためである。
発火版の学習が非自明である以上、配線と多脳化の比較を先に成立させ、発火への移行は G4 として別に検証する。

学習するパラメータはニューロンごとに次の四つである。

| パラメータ | 意味 | 範囲 | 初期値 |
|---|---|---|---|
| g_i | 入力増幅率 | 0.1〜10（対数パラメータ化） | 1.0 |
| b_i | 定常バイアス | −2〜2 | 0 |
| τm_i | 膜時定数 | 10〜200 ms | 20 ms |
| τs_i | 入力電流の時定数 | 5〜50 ms | 5 ms |

ニューロン数を N とすると、神経中核の学習パラメータは 4N 個である。
時定数と不応期の初期値は、全脳 LIF モデルの先行研究 [S14] の膜時定数 20 ms、シナプス時定数 5 ms、不応期 2.2 ms に倣う。
連続値版は Δt が 20 ms なので時定数の分解能は粗いが、範囲は生物学的に妥当な幅に置き、LIF 版でそのまま使えるようにする。

数値安定化として v_i を [−5, 5] に切り詰め、非数が出た場合はチェックポイントを保存して停止する。
初期化時のスペクトル半径、活動の平均と飽和率は M0 と M2 で記録する。

配線の性質を生かす設定として、次の二つを条件として持つ。

- **細胞型による共有**：`mono_typed` では g、b、τm、τs を細胞型ごとに共有する。連続値版の先行研究 [S10] が細胞型単位のパラメータで視覚系の活動を再現しているため、初版のニューロン単位のパラメータ化との差を測る。
- **神経修飾**：初版で 0 とした修飾性伝達物質を、後続で受け手側の増幅率 g_i を動かす経路として入れる。初版では設計上の接続点だけを確保する。

### 4.3 入力ポートの割当

観測の各信号を、MaleCNS の感覚ニューロンの注釈（受容器の種別、入口となる神経、体節、左右）で選んだニューロン群に流し込む。
選択規則は `ports.json` を生成するスクリプトに書き、規則と実際に選ばれたニューロン数を記録する。

MaleCNS の注釈クラスと、この計画での役割の対応は次のとおりである。

| 注釈クラス | 役割 | 節 |
|---|---|---|
| 感覚ニューロン（脚、触角、平均棍、体幹の受容器） | 身体感覚の入力ポート | §4.3 |
| 視覚投射ニューロン（LC 群、LPTC 群） | 深度入力の注入先。初版では入力なしで神経中核の一部 | §9 |
| 視覚葉内在ニューロン | ニューロン集合から除外 | §4.1 |
| 下行性ニューロン | 脳から神経索への内部経路。`tri_linked` では他脳への送り手 | §6.1 |
| 上行性ニューロン | 神経索から脳への内部経路。`tri_linked_an` では他脳への送り手 | §6.3 |
| 運動ニューロン（標的筋の注釈つき） | トルクの出力ポート | §4.4 |
| 上記以外の中間ニューロン | 神経中核の内部。ポートにしない | §4.2 |

身体側の四肢とハエの脚は次のように対応づける。
ハエの脚の関節は胸部と基節の間（3 自由度相当）、基節と転節の間（1）、腿節と脛節の間（1）、脛節と跗節の間（1）で [S19]、人の四肢の自由度構成に近い。

| 人型 | ハエ側の体節 | 関節の対応 |
|---|---|---|
| 左右の腕 | 中脚（T2）、同側 | 肩を胸部基節間、肘を腿節脛節間、手首を脛節跗節間に対応 |
| 左右の脚 | 後脚（T3）、同側 | 股を胸部基節間、膝を腿節脛節間、足首を脛節跗節間に対応 |
| 腰 | 腹部体節 | 腹部の屈伸と側屈 |
| 首 | 首 | 首の運動 |

腕を中脚に対応づけるのは、脚の固有感覚ニューロンが前脚に少ないためである。
M0 の実測では、脚の弦音器は後脚 193、中脚 163 に対して前脚 36 で、毛板も同じ傾向だった。
前脚（T1）と翅、平均棍の運動系は初版では使わず、未使用として記録する。

信号ごとの受容器クラスは次のとおりである。

| 身体側の信号 | ハエ側の受容器クラス | 備考 |
|---|---|---|
| 四肢の関節角度（22） | 対応する脚関節の腿節弦音器（位置応答）と毛板 | 同じ脚、同じ関節 |
| 四肢の関節角速度（22） | 腿節弦音器（方向応答） | 正負で別集団 |
| 腰と首の角度と速度（10） | 首は前胸神経（PrN）の毛板、腰は腹部の固有感覚ニューロン | 注釈に該当がなければ T1 の腿節弦音器を代用し、代用を記録 |
| 接触（12） | 対応する脚の鐘状感覚子と跗節の剛毛。体幹と頭は胸部と腹部の剛毛 | 荷重と触覚 |
| 重力方向（3） | 触角のジョンストン器官の重力と気流に応答する群 [S11] | |
| 骨盤の角速度（3） | 平均棍の鐘状感覚子 | 角速度受容器として |
| 骨盤の線速度（3）と高さ（1） | ジョンストン器官の気流応答群の未使用ニューロン | 生物学的対応がない信号として記録 |
| 視覚（拡張） | 視覚投射ニューロン | §9 |

各スカラー信号は 8 個のガウス型応答曲線で集団符号化する。
中心は正規化範囲 [−1, 1] に等間隔、幅は 0.35 とする。
選ばれたクラスの各ニューロンに、乱数で一つの応答曲線を割り当て、ニューロンごとの非負の注入増幅率 a_i（初期値 1、学習対象）を掛けて u_i とする。
このため入力側の学習パラメータはポートニューロン数と同数で、観測から出力へ直接抜ける経路は存在しない。

### 4.4 出力ポートの読出し

関節 j の正規化トルク指令 a_j は、対応する運動ニューロン群 P_j の活動から次で読み出す。

```text
a_j = tanh( Σ_{i ∈ P_j} w_ji · r_i )
```

r_i は連続値版では s_i、LIF 版では発火の平滑化痕跡である。
P_j は MaleCNS の運動ニューロン注釈（標的筋）から選ぶ。
w_ji は学習対象で、主動筋の運動ニューロンには +1/|主動筋群|、拮抗筋には −1/|拮抗筋群| を初期値として与える。
学習で符号は制約しないが、初期値に筋の役割を反映させる。

群の名前は MaleCNS の `type` 注釈にある標的筋の表記で書く [S12]。
括弧内は M0 で実測した、後脚（T3）片側あたりの該当ニューロン数の目安である。

| 人型の関節 | 主動筋群（+） | 拮抗筋群（−） |
|---|---|---|
| 肩と股の x 軸 | `Tergopleural/Pleural promotor MN`、`Tr flexor MN`（T3 で 6） | `Pleural remotor/abductor MN`（4）、`Tr extensor MN`（3） |
| 肩と股の y 軸 | `Sternal adductor MN`（2） | `Pleural remotor/abductor MN`（4） |
| 肩と股の z 軸 | `Sternal anterior rotator MN`（4） | `Sternal posterior rotator MN`（8） |
| 肘と膝 | `Ti flexor MN`（17）、`Acc. ti flexor MN`（16） | `Ti extensor MN`（4） |
| 手首と足首の x 軸 | `Ta depressor MN` | `Ta levator MN` |
| 足首の y 軸 | `ltm MN`、`ltm1-tibia MN`、`ltm2-femur MN`（別の重みで共有） | 同じ群を負側にも置く |
| 腰の三軸 | 腹部運動ニューロン（`MNad*`）の背側群 | 同じく腹側群。左右差は同側と対側で分ける |
| 首の二軸 | 頸神経の運動ニューロン（`CvN*`）と `GNG*` の運動ニューロン | 同じ群を負側にも置く |

運動ニューロン群は小さい。
1 関節あたりの主動筋群が数ニューロンしかない場合、読出しの表現力が群の大きさで決まってしまうので、次の集約規則を置く。
主動筋群または拮抗筋群が 8 ニューロン未満の関節では、同じ体節、同じ側の脚運動ニューロン全体を P_j に加える。
加えたニューロンの初期重みは 0 とし、標的筋の注釈がある群だけに ±1/|群| の初期値を与える。
注釈に該当する群がまったくない関節は、同じ体節の脚運動ニューロン全体だけを P_j とし、初期重みは 0 とする。
どの関節でどちらの規則を使ったかは `ports.json` に記録する。
群の重なりは許すが、重み w_ji は関節ごとに別に持つ。

PPO の行動分布は、tanh を通す前の値に対するガウス分布とし、標準偏差は関節ごとに学習する（初期値 0.3）。
`tri_symmetric` では各脳が出した a_j とその確率を記録して学習し、平均後の値を各脳の行動として扱わない。

### 4.5 ニューロン集合の規模と計算量

M0 で §4.1 の規則を MaleCNS v1.0 に適用した結果は次のとおりである（`data/m0_scale.json`）。
MaleCNS 全体は約 166,700 ニューロンで [S23]、そのうち `Traced` が 165,122、視覚葉を除いた集合が N = 71,618 である。
集合内の接続はシナプス数 2 以上で nnz = 8,053,558（シナプス 7.1×10^7 個）、入次数の平均は 114、99 パーセンタイルは 618、出次数の最大は 4,034 である。
符号は ACh 45,979、GABA 11,255、Glu 10,106、ヒスタミン 14、修飾性 531 で、細胞型の多数派でも決まらなかった 3,733（5.2%）が +1 になる。

- **重み**：疎行列（CSR、fp16 の値と int32 の列番号）で nnz × 6 バイト = 48 MB。全脳が読み取り専用で共有する。
- **状態**：環境ごと、脳ごとに v、I、s の 3N 個の fp16 で 6N バイト = 430 KB。
- **時系列学習の履歴**：切り詰め窓 T 身体ステップ分の状態を保持する。T = 16、条件あたり 64 環境、脳数 10（`mono` 1、三脳条件 3×3）で 10 × 64 × 16 × 430 KB ≒ 4.4 GB。GPU メモリは RTX 5080 の 16 GB [S6] なので、条件あたりの環境数は 64 を基準とし、条件ごとに更新を順に回す。128 環境（8.8 GB）は物理計算と MLP 価値関数の分を実測してから判断する。
- **LIF 版の履歴**：神経更新が 20 倍になるため、身体ステップ境界の状態だけを保存し、逆伝播時に 20 回の更新を再計算する。履歴のメモリは連続値版と同じで、計算が約 2 倍になる。
- **順伝播の計算量**：一回の神経更新は環境数 B に対して 2 · nnz · B FLOP の疎行列積である。条件あたり 64 環境、脳数 10 で B = 640 相当、10 GFLOP になり、実効 10 TFLOP/s なら 1 ms である。連続値版は身体ステップごとに一回なので、神経中核の順伝播は身体ステップあたり数 ms に収まり、計算予算は物理計算と逆伝播で決まる見込みである。LIF 版はこの 20 倍になる。

MLP 方策の公開実装では A100 で約 1×10^5 身体ステップ/s が報告されている [S18]。
神経中核の疎行列積の実効性能はまだ実測していないので、M0 では推論だけでなく、物理計算、経験収集、逆伝播、更新、評価を含めた身体ステップ/s を実測する。
実測値から本比較の並列数と所要日数を決める（§12）。

## 5. 課題と成功判定と報酬

### 5.1 二つの課題

| 課題 | 開始状態 | 扱い |
|---|---|---|
| `stand_balance` | 立位付近に小さな揺らぎを与える | 実装確認と G4 に使う。起き上がりの成功には数えない |
| `rise_and_stand` | 仰向け、うつ伏せ、横向きで床上から開始 | 本課題。未学習の重みから始める |

`stand_balance` の学習済み重みを本課題へ持ち込まない。
易しい姿勢から段階的に学ばせる方式を試す場合は別条件とし、事前学習の経験量も予算に含める。

`rise_and_stand` の試行は 60 秒で終了し、床への接地や転倒では終了しない。
場外（骨盤が原点から 3 m 以上離れる）と数値異常でも終了する。

### 5.2 成功と習得

一試行の成功は、60 秒以内に次の状態を連続 10 秒満たすことである。

- 両足が床に接し、足以外は床に接していない。
- 胴体の傾きが鉛直から 15 度以内で、骨盤高が基準立位の 90% 以上である。
- 骨盤の水平速度が 0.2 m/s 以下である。

起き上がる途中の手と膝の接地は許す。
「初めて 1 秒、3 秒、10 秒立った」時点は観察用の節目として別に保存する。

評価は学習に使わない 24 個の開始条件（仰向け、うつ伏せ、横向きを 8 個ずつ）で行い、5×10^6 身体ステップごとに実施する。
20 試行以上成功した評価が 2 回続いた時点を **習得** とする。
初成功と習得は区別する。

### 5.3 報酬

各身体ステップの報酬は次の項の和とする。
係数は初期値で、M1 の `mlp_control` による検証で固定し、本比較中は変えない。

| 項 | 式 | 係数 |
|---|---|---|
| 高さの進歩 | (h_t − h_{t−1}) / h_ref | 2.0 |
| 直立性 | cos(胴体傾き)、骨盤高が基準の 50% 以上のときのみ | 0.2 |
| 立位維持 | 成功条件を満たすステップごとに 1 | 1.0 |
| 初回成功 | 連続 10 秒を初めて満たしたとき一回 | 10.0 |
| トルク抑制 | −mean(a_j²) | 0.005 |
| 操作変化の抑制 | −mean((a_j,t − a_j,t−1)²) | 0.01 |
| 立位後の余分な接触 | −（足以外の接触数）、骨盤高が基準の 80% 以上のときのみ | 0.05 |

全脳に同じ全身の報酬を返す。
共有報酬の下で協調が生じるかどうかも検証対象である。

報酬の抜け道として、跳び続ける、膝立ちで止まる、床に引っ掛かって高さを稼ぐ、の三つを M1 で試験する。
報酬の高さと成功判定は分離する。

## 6. 脳間結合の設計

### 6.1 合成シナプスによる結合

`tri_linked` の脳間通信は、脳 A の下行性ニューロンの軸索を脳 B の腹側神経索へ人工的に接続する **合成シナプス** で実現する。
行動として出力する連続値ベクトルは使わない。
理由は二つある。
第一に、連続値ベクトルでは通信の意味が神経配線と無関係な符号になり、コネクトーム実験としての解釈ができない。
第二に、合成シナプスなら通信内容が神経活動そのものになり、どの細胞型が通信に使われたかを配線の言葉で読める。

結合の規則は次のとおりである。

- **送り手**：脳 A の下行性ニューロン全体。細胞型ごとに数を記録する。
- **受け手**：送り手の細胞型 X に対して、脳 B 自身の型 X のニューロンが腹側神経索で結ぶ後シナプス側ニューロン。つまり、脳 B が自分の型 X から受けるのと同じ結線を、脳 A の型 X からも受ける。
- **重み**：脳 B 自身の対応するシナプス数から §4.1 と同じ式で基準重みを作り、結合比率 ρ（初期値 0.25）を掛ける。符号は送り手の神経伝達物質で決める。ρ は合成シナプスごとの非負の学習パラメータとし、対数パラメータ化で 0.01〜4 の範囲に置く。
- **遅延**：連続値版では 1 身体ステップ（20 ms）、LIF 版では 5 神経更新（5 ms）とする。
- **向き**：三脳の全ての順序対（6 本）に同じ規則で張る。

生物との対応で言えば、実際のハエでは一つの脳の下行性指令が全ての脚に届く。
`tri_linked` では、各脳の指令が他の二脳の神経索にも比率 ρ で漏れ出す構造になる。

受け手の入力は §4.2 の m_i に入る。
リセット時には遅延待ち行列も消去し、チェックポイントには待ち行列を含める。

### 6.2 学習と勾配の扱い

合成シナプスの重みは受け手の脳に属し、受け手の方策損失で更新する。
送り手側の活動には、受け手の損失からの勾配を合成シナプスを通して流す。
これは通信路を微分可能にする多エージェント学習 [S13] と同じ考え方で、送り手が「相手に使われる信号」を学習するために必要である。
勾配を流さなければ、送り手の内部活動は自分の行動を改善する更新の副産物としてしか変わらず、相手に役立つ信号へ向かう圧力がかからない。

勾配を流しても、各脳の学習パラメータと最適化状態は分けたままである。
脳の独立性の定義は「パラメータと状態を共有しない」であり、「勾配が流れない」ではない。
勾配の倍率 λ は初期値 1 とし、λ = 0（受け手だけが学習する）を対照として持つ。

### 6.3 通信内容の解析

通信の効果と内容は次で測る。

- **切断**：評価用コピーで合成シナプスを 0 にし、性能変化を測る。残りの入力を自動増幅しない。
- **時間の入替**：送り手の活動を時間方向に入れ替えて流し、内容の有用性と容量増加を分ける。
- **環境の入替**：別環境の送り手活動を流し、同じことを別の方法で測る。
- **使われた細胞型**：ρ の成長と活動の相関から、通信に使われた下行性ニューロン型を列挙する。
- **相互情報量**：送り手の型ごとの活動と、受け手の 1 ステップ後のトルク変化との相互情報量を推定する。

これらは §8 の監視記録から計算する。
本比較の後、上行性ニューロンを送り手にした変種（感覚共有）を `tri_linked_an` として追加する。

## 7. 学習処理

PyTorch と TorchRL で PPO を実装し、学習時だけ全身の状態で価値を評価する MAPPO 方式 [S3][S4] を使う。
各脳の行動選択モデル（神経中核とポートと読出し）は独立で、最適化状態も分ける。
価値関数は観測 76 次元と直前の適用トルク 27 次元を入力とする 2 層 256 ユニットの MLP で、実行時の動作には関与しない。

時系列の学習は切り詰め窓で行う。

| 設定 | 初期値 |
|---|---|
| 切り詰め窓 T | 16 身体ステップ |
| 収集長 | 64 身体ステップ |
| 更新回数 | 収集ごとに 4 エポック |
| クリップ幅 | 0.2 |
| 割引率、GAE の λ | 0.99、0.95 |
| 学習率 | ポートと読出しは 3×10^−4、神経中核は 1×10^−4（Adam） |
| エントロピー係数 | 0 |
| 勾配ノルムの上限 | 1.0 |
| 観測の正規化 | 移動統計、評価時は固定 |

窓の開始時の内部状態、直前履歴、過去の行動確率を保存して再計算し、時系列を一枚ずつ無作為に混ぜない。
全ニューロン、全時刻の履歴を無制限に GPU へ保持しない。

ハイパーパラメータ探索の範囲は次に限定する。
探索は `mono` の `stand_balance` で行い、各設定 5×10^7 身体ステップ、1 乱数で走らせる。
`mlp_control` は神経中核に固有の軸を持たないため、学習率とエントロピー係数の 4 通りだけを同じ予算で試す。

| 軸 | 候補 |
|---|---|
| 神経中核の学習率 | 1×10^−4、3×10^−4 |
| エントロピー係数 | 0、1×10^−3 |
| 増幅率 g の初期スケール | 0.5、1.0 |
| 膜時定数 τm の初期値 | 20 ms、50 ms |

16 通りの上位 3 設定を `rise_and_stand` で 2×10^8 身体ステップずつ試し、一つを選んで全条件に適用する。
本比較中は条件ごとの個別調整をしない。
探索に使った経験量は本比較の予算とは別に記録する。

## 8. 監視と内話モニタ

### 8.1 記録の形式

三脳の競合と協調、通信の中身、報酬の推移を、学習中に読める形で残す。
記録は 1 行 1 事象の JSON Lines とし、`runs/<run>/monitor/events.jsonl` に書く。
全環境を全時刻で記録すると量が膨らむため、二つの粒度に分ける。

- **注視個体**：条件ごとに 1 環境を選び、全身体ステップ（50 Hz）で記録する。
- **集計**：全環境について 1000 身体ステップごとに平均、分散、最大を記録する。

注視個体の一事象は次の項目を持つ。

| 項目 | 内容 |
|---|---|
| `t`、`env`、`condition`、`brain` | 時刻（身体ステップ）、環境、条件、脳 |
| `proposal` | 脳が出した 27 次元のトルク案（`tri_symmetric` は平均前） |
| `applied` | 身体に適用されたトルク |
| `reward` | §5.3 の項ごとの値 |
| `contact` | 接触している部位の集合 |
| `link` | `tri_linked` のみ。送り手の型ごとの活動上位 8 件と、受け手側の結合比率 ρ の変化 |
| `core` | 活動の平均、飽和率、発火率（LIF 版）、ポートと読出しの活動統計 |

### 8.2 協調と競合の指標

記録から次の指標を計算し、集計にも含める。

- **競合指数**（`tri_symmetric`）：関節ごとに三脳の案の標準偏差を案の絶対値の平均で割った値。
- **一致度**：脳の対ごとの案のコサイン類似度。
- **支配率**：適用トルクへの各脳の寄与の絶対値の割合。
- **左右の同期**（`tri_body`、`tri_linked`）：左右の股と肩のトルクの相互相関とその遅れ。
- **通信の使用度**（`tri_linked`）：型ごとの ρ の平均と、送り手活動と受け手トルク変化の相互情報量。

協調と役割分化は活動の相関だけで判断しない。
評価用コピーで一脳の出力を 0 にする、結合を切る、左右から小さな外力を与える、といった介入前後の性能変化を測る。

### 8.3 内話としての語り

指標を人が読める文にする **語り手** を、監視記録を読む独立したモジュールとして置く。
初版は規則に基づく雛形で、閾値を越えた事象を一行の文にする。

```text
t=12.3s  左脳: 右股関節に伸展 +0.8 を要求 / 中央脳: 同関節に −0.5 → 競合指数 1.6
t=12.4s  右脳→中央脳: DN 型 DNp09 の活動が上位に入る（ρ 平均 0.41、上昇中）
t=13.0s  初めて 1 秒の立位を達成（試行 3、開始姿勢: うつ伏せ）
```

語り手は事象の形式だけに依存し、学習処理の内部には触れない。
このため、後で言語モデルによる語り手に差し替えても学習側は変わらない。
表示側（§11）は語りの文をアバターの横に流す。

## 9. 視覚経路の拡張

身体感覚だけでなく、低解像度の深度情報を視覚投射ニューロンに流す経路を、本比較の後の拡張条件として設計する。
視覚葉そのものは初版のニューロン集合から除いてあるため、深度は視覚葉の出力側に注入する。

- **センサ**：頭部の `<site>` に `rangefinder` センサを左右の眼ごとに 8×6 本並べ、方位 ±60 度、仰角 ±40 度を等間隔に覆う。到達距離は 5 m とし、値は 1 − d/5 で正規化する。
- **注入先**：視覚投射ニューロン（視小葉の LC 群と視小葉板の LPTC 群）。各ニューロンの受容野の中心は、視覚葉で接続する柱の座標から方位と仰角に変換して求める。
- **変換**：LC 群には受容野に最も近い測距値と、その時間差分を入力する。LPTC 群には測距グリッドの時間差分を、細胞型ごとの優先方向で重みづけた値を入力する。注入増幅率は §4.3 と同じ非負の学習パラメータとする。
- **条件**：`mono_vision` と `tri_linked_vision` を M5 で試行する。視覚の有無以外は本比較と同じ設定である。

MuJoCo Warp は `rangefinder` センサと、深度画像を出す並列レンダラの両方を持つ [S5]。
初版は計算の軽い `rangefinder` を使い、解像度を上げる場合は `<camera output="depth">` による並列レンダラへ切り替える。
測距値を受容野へ束ねる方式は、ハエの身体シミュレータが画像を個眼格子に束ねる方法 [S20] に倣う。

## 10. 並列実行と計測

### 10.1 三つの単位を混同しない

| 単位 | 共有するもの | 独立させるもの |
|---|---|---|
| 同一条件、同一試行の並列環境 | 学習中のモデル | 身体、神経活動、乱数、結合の遅延待ち行列 |
| 一つの身体に載る三脳 | 固定の元配線だけ | 学習パラメータ、神経活動、操作出力 |
| 条件違い、独立した再試行 | 元データと実験仕様 | 重み、経験、最適化状態、正規化統計 |

多数の環境から一つのモデルへ経験を集めても、独立した再試行の数が増えたことにはならない。
本比較は各条件につき 3 種類の学習用乱数から始め、個々の結果をすべて表示する。

### 10.2 実行方式

一つの管理プロセスで 5 条件を起動し、各条件の収集と更新を小さな単位で交互に進める。
読み取り専用の元配線を共有し、条件間の学習データは分ける。
各条件を最後まで順番に走らせる方式にはしない。

単一 GPU 上で全条件が常に同時実行されることは要求しない。
全条件を継続して進める並行スケジュールを実現し、余裕がある部分をまとめて GPU 処理する。
一度に全乱数試行が載らない場合も、各試行の組の中では 5 条件を並行して進める。

物理計算は環境数を `nworld` に指定した一つの MuJoCo Warp データで進め、一歩分を CUDA グラフに取り込んで起動する [S5]。
物理状態と神経状態の受け渡しは Warp と PyTorch のゼロコピー変換で行い、ホストを経由しない。
接触と拘束のバッファ（`nconmax`、`njmax`）は固定長なので、M0 で起き上がり動作中の最大接触数を測って余裕を持たせ、溢れを示すフラグを毎ステップ監視する。

### 10.3 計測

条件あたり 64 環境を基準とし、全条件を載せた状態で 16、32、64、128 を測る。
推論だけでなく、物理計算、経験収集、神経中核の逆伝播、更新、評価まで含めて測る。

記録する値は、最大 GPU メモリ、ホストメモリ、条件別の身体ステップ/s、更新時間、保存時間、表示の有無である。
GPU メモリに 15〜20% の余裕を残す方針とし、最大並列数だけを目的にしない。

`benchmark.json` から並列数を決め、長時間実行の前に `max_body_steps` と `max_compute_hours` を必須設定とする。
未設定なら長時間実行を開始しない。
数値異常とメモリ不足ではチェックポイントを保存して停止する。

### 10.4 時間の数え方

経過した実時間、条件に割り当てた実計算時間、累積の身体ステップ、合計の仮想経験時間を分けて記録する。
三脳が同じ身体を一回操作した経験は 1 身体ステップとして数える。
経験量をそろえた曲線と、計算時間をそろえた曲線を両方作る。
計算時間は他条件と重ならない計測区間で集計し、評価と表示の負荷も別記する。

## 11. 記録と可視化

最低限、成功率、連続立位時間、起き上がり時間、姿勢と高さ、接触、トルク、各脳の出力と活動の集計、結合の活動、三種類の時間指標を保存する。
全時刻の全ニューロン活動は保存せず、集計値、抽出した神経、節目の短い記録に絞る。

各条件を一体ずつ横並びで表示し、同じ経験量のチェックポイントを再生できるようにする。
実時間表示と、学習量をそろえた再生を区別する。
選択個体の表示が停止しても学習は続く構成とする。

VRM は保存された物理姿勢を表示する。
演出で立位を補正せず、実測値に基づいて節目を表示する。
VRM モデルは利用条件を確認したものを使う [S7]。

設定、コードの版、データのハッシュ、乱数、モデル、最適化状態、正規化統計をチェックポイントへ保存する。
継続用には身体、神経、結合の状態も含める。
GPU 計算の完全な一致は保証せず、同条件の再評価による再現性を確認する [S5]。

## 12. 実装の分割とマイルストーン

### 12.1 構成

```text
configs/                 # 条件、課題、報酬、予算、joint_groups.yaml
src/multibrain/
  data/                  # 取得、検証、疎行列化、ports.json の生成
  brain/                 # 更新式、ポート、読出し、合成シナプス
  body/                  # humanoid.xml、観測、操作、成功判定
  learning/              # PPO/MAPPO、切り詰め窓の学習処理
  runner/                # 並行実行、計測、保存と再開
  monitor/               # 事象記録、指標、語り手
  evaluation/            # 共通評価、介入、比較集計
viewer/                  # VRM 表示、記録の再生、語りの表示
assets/                  # 身体定義、利用条件を確認した表示資産
runs/                    # 設定、結果、記録。Git 管理対象外
tests/                   # 単体試験、隔離試験、動作確認
```

Python、PyTorch、TorchRL、MuJoCo Warp の版は導入時の互換性試験で固定する。
最初は Linux 上のローカル実行を対象とし、他 OS 対応や独自 CUDA 実装は後回しにする。
CPU は取得、前処理、管理、保存を、GPU は神経計算、学習、物理計算を担当する。

### 12.2 マイルストーン

| 段階 | 作業 | 次へ進む条件 |
|---|---|---|
| M0 データと計測 | データ固定、`ports.json` 生成、身体定義の受理確認、接触バッファの上限測定、全規模の順伝播と逆伝播、身体ステップ/s の実測 | `dataset_manifest.json`、`ports.json`、`benchmark.json` があり、全規模で一回の学習更新が通る |
| M1 身体と MLP | `humanoid.xml`、観測、成功判定、報酬、`mlp_control` | §3.8 の検証を通し、G1 を達成する |
| M2 一脳 | 更新式、ポート、読出し、切り詰め窓の学習、保存と再開、ハイパーパラメータ探索 | 小規模試験で勾配を検証し、全規模で中核のパラメータへ更新が届き、G2 を達成する |
| M3 三脳と結合と監視 | 三脳条件、操作分配、合成シナプス、監視記録、語り手 | 全条件が継続して進み、学習状態、経験、結合の混線がなく、切断と入替の介入が動く |
| M4 本比較 | 5 条件 × 3 乱数 × 5×10^8 身体ステップ、対照条件、介入試験、VRM 再生 | G3 を達成し、成功と未達成を含む結果を再生できる |
| M5 LIF と視覚 | 出力関数の切替、代理勾配、`rangefinder`、視覚投射ニューロンへの注入 | G4 を達成し、視覚条件が学習を開始できる |

所要日数は M0 の実測値から次の式で見積もる。

```text
日数 = 総身体ステップ / (身体ステップ/s × 86400)
```

主比較 5 条件の総身体ステップは 7.5×10^9 で、全条件合計で 2×10^4 身体ステップ/s が出れば約 4.3 日である。
対照条件 4 種を同じ予算で加えると 1.35×10^10 になり、約 7.8 日である。
この速度が出なければ、環境数を減らすか、条件を組に分けて順に回す。
実測値は `benchmark.json` に残し、見積もりと実績の差を記録する。

各段階は小規模の試験を先に通す。
ただし、縮小した神経回路や学習停止モデルの結果を、全規模で中核学習ありの結果として扱わない。

必須試験は、関節担当の重複と漏れ、成功判定の抜け道、三脳の独立性、環境リセット、結合の遅延、時系列の行動確率、保存と再開、表示を止めた場合の学習継続、連続値版と LIF 版の一致試験とする。

### 12.3 診断手順

`mlp_control` が G1 に達しなければ、身体、報酬、学習処理を先に直す。
神経モデルだけが改善しなければ、次の順で調べる。

1. 入力の到達性：ポートから読出しまでの経路の有無と長さを配線上で確認する。
2. 活動の飽和：平均活動と飽和率を見て、c と g の初期値を見直す。
3. 勾配：中核のパラメータに届く勾配のノルムを層ごとに記録する。
4. 学習範囲：`mono_frozen` と比べ、中核の学習が寄与しているかを見る。
5. 読出し：`mono` の読出しだけを MLP に置き換えた試験で、読出しの表現力が足りているかを見る。
6. 学習の出発点：それでも改善しなければ、`mlp_control` の行動を模倣学習で写してから強化学習に入る `mono_bc` を追加する。先行研究 [S16] は配線由来の方策を模倣学習で初期化してから PPO で仕上げており、模倣に使った経験量は予算に含めて記録する。

予算内で立てなかった場合は「未達成」と記録し、上の手順で得た診断結果を添える。

「初期値」と記した数値は M1 までに固定し、以後の比較中は変えない。
変更した場合は版を上げて記録する。

## 参考資料

[S1] から [S7] は版 0.1 で 2026-09-16 に確認した資料である。
[S8] 以降は版 0.2 で追加した資料で、URL を付したものは 2026-09-16 に取得したページで確認した。
MuJoCo の文書は readthedocs を直接取得できなかったため、同じ内容の文書ソース（GitHub 上の `doc/` 以下）で確認した。
「要確認」と付したものは原典を取得できておらず、M0 の開始前に原典を確認し、数値と主張の対応を取り直す。

- **[S1] HHMI Janelia, Male CNS Connectome**：対象範囲、公開版、利用条件。公式記録では v1.0 公開は 2026-06-08、正式論文公開は 2026-09-03。
  `https://www.janelia.org/project-team/flyem/male-cns-connectome`
- **[S2] MaleCNS 公式ダウンロード**：神経情報、伝達物質予測、集約済み接続表。
  `https://male-cns.janelia.org/download/`
- **[S3] Yu et al., The Surprising Effectiveness of PPO in Cooperative, Multi-Agent Games**：協調型 PPO の基礎。
  `https://arxiv.org/abs/2103.01955`
- **[S4] TorchRL, Multi-Agent Reinforcement Learning (PPO)**：中央評価と分散した行動選択、パラメータ共有の設定。
  `https://docs.pytorch.org/rl/stable/tutorials/multiagent_ppo.html`
- **[S5] MuJoCo, MuJoCo Warp**：GPU 並列物理計算、PyTorch との接続、再現性の制約。物理計算の微分は前提にしない。
  `https://mujoco.readthedocs.io/en/stable/mjwarp/index.html`
  `https://github.com/google-deepmind/mujoco_warp`（対応機能の一覧、一次遅れアクチュエータ、`rangefinder`、並列レンダラ）
- **[S6] NVIDIA, GeForce RTX 5080**：16 GB GDDR7 等の公式仕様。版 0.2 までは RTX 5090（32 GB）を想定していたが、M0 で実機を確認して改めた。
  `https://www.nvidia.com/en-us/geforce/graphics-cards/50-series/rtx-5080/`
- **[S7] VRM Public License 1.0**：モデルごとの利用条件と表示条件。
  `https://vrm.dev/en/licenses/1.0/`
- **[S8] de Leva, Adjustments to Zatsiorsky-Seluyanov's segment inertia parameters, Journal of Biomechanics, 1996**：体節の質量比（要確認）。
- **[S9] Neftci, Mostafa, Zenke, Surrogate Gradient Learning in Spiking Neural Networks, 2019**：離散時間 LIF（減算リセット）と代理勾配。
  `https://arxiv.org/abs/1901.09948`
- **[S10] Lappalainen et al., Connectome-constrained networks predict neural activity across the fly visual system, Nature, 2024**：細胞型単位のパラメータを持つ連続値の配線制約モデル（要確認）。
- **[S11] Kamikouchi et al., The neural basis of Drosophila gravity-sensing and hearing, Nature, 2009**：ジョンストン器官の重力と気流に応答する群（要確認）。
- **[S12] Azevedo et al., A size principle for recruitment of Drosophila leg motor neurons, eLife, 2020、および Cheong et al., Transforming descending input into behavior, eLife, 2024**：脚運動ニューロンの標的筋による分類（要確認）。
- **[S13] Foerster et al., Learning to Communicate with Deep Multi-Agent Reinforcement Learning, NeurIPS, 2016**：微分可能な通信路による多エージェント学習（要確認）。
- **[S14] Shiu et al., A Drosophila computational brain model reveals sensorimotor processing, Nature, 2024**：全脳配線に対する LIF モデル。静止電位 −52 mV、閾値 −45 mV、膜時定数 20 ms、シナプス時定数 5 ms、不応期 2.2 ms、1 シナプスあたり 0.275 mV、グルタミン酸は抑制性。
  `https://www.nature.com/articles/s41586-024-07763-9`
- **[S15] MuJoCo, humanoid モデル**：27 自由度（根の 6 を含む）、21 アクチュエータ、`gear` と関節属性の基準値。
  `https://github.com/google-deepmind/mujoco/blob/main/model/humanoid/README.md`
- **[S16] Jin et al., Whole-Brain Connectomic Graph Model Enables Whole-Body Locomotion Control in Fruit Fly, arXiv, 2026**：FlyWire 配線を非発火のグラフ方策としてハエ身体の制御に使い、模倣学習の後に PPO で仕上げた先行例。付録に LIF 版の基準実装が学習に失敗した報告がある。
  `https://arxiv.org/abs/2602.17997`
- **[S17] He et al., Learning Getting-Up Policies for Real-World Humanoid Robots, RSS, 2025**：床からの起き上がりの二段階学習。4,096 環境、第一段階 5×10^9 ステップ。
  `https://arxiv.org/abs/2502.12152`
- **[S18] Zakka et al., MuJoCo Playground, 2025**：MLP 方策の学習設定（人型の立位維持は PPO 6×10^7 ステップ、2,048 環境）とスループットの公開値。人型の起き上がり課題は含まれない。
  `https://arxiv.org/abs/2502.08844`
  `https://github.com/google-deepmind/mujoco_playground`
- **[S19] Vaxenburg et al., Whole-body physics simulation of fruit fly locomotion, Nature, 2025**：102 自由度のハエ身体モデルと MLP 方策。配線は使っていない。
  `https://www.nature.com/articles/s41586-025-09029-4`
- **[S20] Wang-Chen et al., NeuroMechFly v2, Nature Methods, 2024**：画像を個眼格子に束ねる視覚入力と、複数感覚を持つハエ身体シミュレータ。
  `https://www.nature.com/articles/s41592-024-02497-y`
- **[S21] snnTorch, Leaky と Synaptic ニューロン、代理勾配の既定値**：減算リセット、arctan と高速シグモイドの傾き。
  `https://snntorch.readthedocs.io/en/latest/snn.neurons_leaky.html`
- **[S22] Topological Sensitivity in Connectome-Constrained Neural Networks, arXiv, 2026**：初期化を共有し次数を保った対照では配線の優位が消えるという報告。
  `https://arxiv.org/abs/2604.04033`
- **[S23] Berg et al., MaleCNS 論文, Cell, 2026**：約 166,700 ニューロン。
  `https://doi.org/10.1016/j.cell.2026.08.015`
